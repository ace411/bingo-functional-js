const concat = require('./concat')
const truncate = require('./truncate')
const slugify = require('./slugify')
const toWords = require('./toWords')

module.exports = { concat, truncate, slugify, toWords }
